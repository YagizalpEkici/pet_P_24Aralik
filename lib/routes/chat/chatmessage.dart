import 'package:flutter/cupertino.dart';
import 'package:pet_project/routes/chat/chatdetailpage.dart';

class ChatMessage{
  String message;
  MessageType type;
  ChatMessage({required this.message,required this.type});
}